## How to configure

* Make sure the path to the git.exe directory is added to ENV PATH
* Open the git exe and enter: git config --global credential.helper wincred
* Open ControlPanel > Windows Credentials and add a new generic credential
